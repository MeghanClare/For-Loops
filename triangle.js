// Write a loop that makes seven calls to console.log to output the following
// its a fucking triangle 
// .length is a property of all strings that can find length 

//for (var text = '#'; text.length <= 7; text = text + '#') {
//	console.log(text); 
//}

// print an 8x8 grid 

for (var text = '#'; text.length < 5; text = text + "#"){
	console.log(text)
}